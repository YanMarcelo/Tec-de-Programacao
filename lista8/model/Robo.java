package model;

import javax.swing.ImageIcon;

public class Robo extends Entidades {
    public Robo(Plano plano, int posicaox, int posicaoy, ImageIcon iconRobo) {
        super(plano, posicaox, posicaoy, iconRobo);
    }
}
